go_package = "main"

tool snake_case_tool {
    description "Test snake case tool name"

    input {
        test_field: string "Test field" required
    }

    output {
        success: bool "Whether the operation succeeded"
        error_message: string "Error message if operation failed"
    }
}